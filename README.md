# Telegram Mini App - bro shop

Telegram бот с мини-приложением для магазина bro.

## Структура проекта

- `bot/` - Telegram бот на aiogram 3.x
- `backend/` - FastAPI бэкенд
- `webapp/` - React мини-приложение (Vite + Tailwind)
- `media/` - Фото товаров

## Быстрый старт

### 1. Настройка окружения

Скопируйте `.env.example` в `.env` и заполните значения:
```bash
cp .env.example .env
```

**Важно:** Заполните следующие переменные:
- `BOT_TOKEN` - получите у [@BotFather](https://t.me/BotFather)
- `ADMIN_IDS` - ваш Telegram user_id (можно узнать у [@userinfobot](https://t.me/userinfobot))
- `DATABASE_URL` - URL базы данных PostgreSQL
- `WEBAPP_URL` - URL веб-приложения (для локальной разработки: `http://localhost:5173`)

### 2. Установка зависимостей

**Python зависимости:**
```bash
pip install -r requirements.txt
```

**Node зависимости:**
```bash
cd webapp
npm install
cd ..
```

### 3. Запуск PostgreSQL

**Вариант 1: Docker (рекомендуется)**
```bash
docker-compose up -d postgres
```

**Вариант 2: Локально**
Установите PostgreSQL и создайте базу данных `broshop`

### 4. Инициализация базы данных

```bash
cd backend
python init_db.py
cd ..
```

### 5. Запуск сервисов

Откройте **3 отдельных терминала**:

**Терминал 1 - Бэкенд:**
```bash
cd backend
uvicorn main:app --reload
```

**Терминал 2 - Веб-приложение:**
```bash
cd webapp
npm run dev
```

**Терминал 3 - Бот:**
```bash
cd bot
python main.py
```

### 6. Настройка Telegram бота

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен и добавьте в `.env`
3. Настройте Web App:
   - Отправьте команду `/newapp` боту @BotFather
   - Выберите вашего бота
   - Укажите название: `bro shop`
   - Укажите URL: `http://localhost:5173` (для локальной разработки)
   - Для продакшена используйте публичный URL

## Использование

### Публичные команды бота:
- `/start` - приветствие и кнопка открытия магазина
- `/catalog` - открыть каталог товаров
- `/hours` - часы работы магазина
- `/location` - адрес магазина
- `/promo` - акции и скидки

### Админ-панель:
- `/admin` - вход в админ-панель (только для пользователей из `ADMIN_IDS`)

**Функции админки:**
- ➕ Добавить товар - пошаговое добавление товара с фото
- ✏️ Редактировать товар - редактирование отдельных полей
- 📊 Статистика - просмотр статистики и ТОП товаров

## Использование Docker (полностью)

```bash
docker-compose up -d
```

## Структура базы данных

- `products` - таблица товаров
- `product_views` - таблица просмотров (уникальные по user_id + product_id)

## Разработка

### Локальная разработка

Для локальной разработки все сервисы запускаются на localhost:
- Backend: `http://localhost:8000`
- WebApp: `http://localhost:5173`

### Продакшен

Для продакшена:
1. Разверните бэкенд на сервере (Render, Railway, VPS)
2. Разверните веб-приложение (Vercel, Netlify, или статический хостинг)
3. Обновите `WEBAPP_URL` в `.env`
4. Настройте Web App URL в @BotFather

## Troubleshooting

**Проблема:** Бот не отвечает
- Проверьте `BOT_TOKEN` в `.env`
- Убедитесь, что бот запущен

**Проблема:** Веб-приложение не загружается
- Проверьте `WEBAPP_URL` в `.env`
- Убедитесь, что веб-приложение запущено на порту 5173
- Проверьте настройки Web App в @BotFather

**Проблема:** Ошибка подключения к БД
- Проверьте `DATABASE_URL` в `.env`
- Убедитесь, что PostgreSQL запущен
- Проверьте права доступа к базе данных
